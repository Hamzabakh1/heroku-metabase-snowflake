build:
  docker:
    web: Dockerfile

release:
  image: web            # ← point at the “web” image you just built
  command:
    - /app/bin/build-driver-cache.sh

run:
  web: /app/docker-entrypoint.sh
