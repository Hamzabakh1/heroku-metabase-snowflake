# heroku.yml
build:
  docker:
    web: Dockerfile

release:
  # Force-release any Liquibase locks
  command:
    - java
    - -jar
    - metabase.jar
    - migrate
    - release-locks

run:
  web: bash ./docker-entrypoint.sh
