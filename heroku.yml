# ── heroku.yml ───────────────────────────────────────────────
build:
  docker:
    web: Dockerfile

release:
  # exécute le script qui télécharge et met en cache le driver
  command:
    - /app/bin/build-driver-cache.sh

run:
  web: /app/docker-entrypoint.sh
